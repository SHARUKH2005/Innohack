import { ShoppingBag, Tag } from "lucide-react";
import { getStudentData } from "@/lib/student-data";

export default async function MarketplacePage() {
  const { listings, errors } = await getStudentData();
  return <main className="mx-auto max-w-6xl px-4 py-8 lg:px-8"><header className="mb-8"><p className="text-[11px] font-bold uppercase tracking-[0.2em] text-[#0056D2]">Trade</p><h1 className="mt-2 text-3xl font-black text-slate-900">Marketplace</h1><p className="mt-2 text-sm text-slate-500">Browse active NFT listings recorded by the platform.</p></header>{errors.length > 0 && <div className="mb-6 rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800">Some marketplace records are unavailable from the current database connection.</div>}<section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{listings.length ? listings.map((listing) => <article key={String(listing.id)} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex h-32 items-center justify-center rounded-xl bg-gradient-to-br from-blue-100 to-indigo-100"><Tag className="h-10 w-10 text-[#0056D2]" /></div><p className="mt-4 text-xs font-bold uppercase tracking-wide text-slate-500">NFT {listing.nft_token_id}</p><p className="mt-2 text-2xl font-black text-slate-900">{Number(listing.price)} MX</p><button className="mt-4 w-full rounded-xl bg-[#0056D2] py-3 text-sm font-bold text-white">View listing</button></article>) : <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-8 text-sm text-slate-500 sm:col-span-2 lg:col-span-3"><ShoppingBag className="mb-3 h-6 w-6 text-slate-400" />No active marketplace listings have been recorded yet.</div>}</section></main>;
}
