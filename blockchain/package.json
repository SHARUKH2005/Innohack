{
  "name": "blocklearnx-contracts",
  "version": "1.0.0",
  "description": "BlockLearnX Smart Contracts for MX Rewards, Soulbound Certificate NFTs, and Course Registry",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.17.0",
    "hardhat": "^2.29.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
