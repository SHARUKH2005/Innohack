import { Request, Response } from "express";

// Marketplace controller — to be implemented
export async function listMarketplaceItems(_req: Request, res: Response) {
  res.status(501).json({ message: "Not implemented yet." });
}